<% if @success %>
  $('<%= "#toggle-status-#{@bank.id}" %>').html("<%= escape_javascript(link_to_with_icon(action_text_to_toggle_bank_status(@bank), action_to_toggle_bank_status(@bank), toggle_activation_admin_bank_path(@bank), {:remote => true, :method => :put})) %>")
<% else %>
  alert('<%= @bank.errors.full_messages.to_sentence %>')
<% end %>
